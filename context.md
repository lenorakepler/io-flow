I would like to be able to build mermaid-like graphs that are interactive, portable (just a .html file), and defined in just json/yaml. Ideally, this would be mostly HTML, and the node-types specified in the json would map to specific HTMl with specific CSS.

I would like to use packages/algorithms to determine layout, but also be able to drag and drop nodes and edges within-browser and have the option to save that layout to the original file.

Also, I'd like to be able to click on a node and dim everything but the nodes/edges directly coming into and out of that node. There should also be an optional sidebar that appears that has more detail.

Ideally, this would be mostly HTML.

An example of the input and architecture I'd want is in example_input.yaml.